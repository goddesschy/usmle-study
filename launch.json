{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "USMLE 서버 실행",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/usmle_server.py",
            "args": [
                "--config", "${workspaceFolder}/config.txt"
            ],
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
